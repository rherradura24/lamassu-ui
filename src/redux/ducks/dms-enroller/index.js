import * as dmsEnrollerReducer from "./Reducer";
import * as dmsEnrollerEpic from "./Epic";
import * as dmsEnrollerActions from "./Actions";

export default {
    reducer: dmsEnrollerReducer,
    epic: dmsEnrollerEpic,
    actions: dmsEnrollerActions
}